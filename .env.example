# Local development only. Real deployments use RDS IAM auth, so no passwords.
DATABASE_URL=postgresql://devcroaker:localdev@localhost:5432/devcroaker

# API
API_PORT=8787

# Web to API base URL. In AWS this is same-origin /api via CloudFront.
NEXT_PUBLIC_API_URL=http://localhost:8787
